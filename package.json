{
  "devDependencies": {
    "concurrently": "^9.0.1",
    "tailwindcss": "^3.4.7"
  },
  "scripts": {
    "dev": "concurrently --names 'NPM,COMPOSER' --prefix-colors 'magenta,yellow' --kill-others 'npx tailwindcss -i ./public/css/tailwind.css -o ./public/css/lib/tailwind.min.css --watch --minify' 'composer start'"
  }
}
